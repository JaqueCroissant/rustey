mod token;

use token::Token;

pub struct Lexer{

}

pub impl Lexer{
    pub fn next_token() -> u8{
        'a'
    }
}

#[cfg(test)]
#[test]
fn it_works() {
    let result = 2 + 2;
    assert_eq!(result, 4);
}